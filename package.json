{
  "name": "Siklo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "web:dev": "pnpm --filter web run dev",
    "web:build": "pnpm --filter web run build",
    "web:lint": "pnpm --filter web run lint",
    "api:dev": "pnpm --filter api start:dev",
    "api:build": "pnpm --filter api run build",
    "api:lint": "pnpm --filter api run lint",
    "shared:build": "pnpm --filter shared-schemas run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.5.1",
      "onFail": "download"
    }
  },
  "type": "module",
  "dependencies": {
    "zod": "^4.4.3"
  }
}
